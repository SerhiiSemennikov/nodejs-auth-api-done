interface ModelDescription {
  title: string;      // A short heading or label
  text: string[];     // Multiple lines or paragraphs of text
}
// Define an array of such items
export type ModelDescriptions = ModelDescription[];